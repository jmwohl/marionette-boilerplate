/**
 * Does nothing more than kick off the application.
 */
require([
        'backbone', 'RevisitAdminApp'
    ],
    function(
        Backbone, App
    ) {
        'use strict';

        App.start();

    });
